import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  activityContainer: {
    height: '100%',
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
